import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'select-chrome-beta',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
